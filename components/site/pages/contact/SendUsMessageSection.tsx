import SectionContainer from '@/components/site/shared/SectionContainer';

const SendUsMessageSection = () => {
  return (
    <SectionContainer id='send-us-message-section' className='bg-white'>
      <h1>Send Us Message Section</h1>
    </SectionContainer>
  );
};

export default SendUsMessageSection;
