import SectionContainer from '@/components/site/shared/SectionContainer';

const ProgramProcessSection = () => {
  return (
    <SectionContainer id='program-process-section' className='bg-white'>
      <h1>Program Process Section</h1>
    </SectionContainer>
  );
};

export default ProgramProcessSection;
