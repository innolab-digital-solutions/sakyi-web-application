/**
 * Admin program list table. Connect to the admin programs list API and shared
 * table utilities when implementing the full listing.
 */
export default function ProgramTable() {
  return (
    <div className='text-muted-foreground rounded-lg border p-8 text-sm'>
      Program listing table placeholder — connect to the admin programs endpoint
      and table hooks.
    </div>
  );
}
