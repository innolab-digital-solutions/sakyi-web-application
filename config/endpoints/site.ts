/**
 * API endpoints that back public/site-facing features.
 *
 * These are typically used by the marketing or informational pages
 * (e.g. contact forms, newsletter signup, public lookups).
 *
 * All paths are relative to the API base URL.
 */
export const SITE_ENDPOINTS = {
  EXAMPLE: '/site/example',
} as const;
